---
interface Props {
  background?: string;
  class?: string;
}

const { background = "white", class: className = "" } = Astro.props;
---

<section class={`py-8 ${background === 'gray' ? 'bg-gray-50' : ''} ${className}`}>
  <div class="container mx-auto">
    <slot />
  </div>
</section>
